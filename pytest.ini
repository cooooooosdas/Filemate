[pytest]
testpaths = filemate/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    e2e: 端到端测试，需要真实 LLM API Key 与 datasets/raw 样本。CI 用 `pytest -m "not e2e"` 跳过，本地手动跑用 `pytest -m e2e`。

# 默认输出：显示 skip/xfail 原因，便于确认 e2e 是"被跳过"而不是"没收集到"
addopts = -ra
