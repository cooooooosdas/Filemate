# ── 感知层 ──
watchdog>=4.0
PyPDF2>=3.0
pdfplumber>=0.10
python-docx>=1.1
python-pptx>=0.6
PaddleOCR>=2.7; platform_system != "Windows"  # Windows 环境按需安装

# ── 理解层 ──
requests>=2.31

# ── 确认层 ──
gradio>=4.44

# ── 执行层 ──
icalendar>=6.0

# ── 开发工具 ──
pytest>=8.0
pytest-asyncio>=0.23
black>=24.0
ruff>=0.5
