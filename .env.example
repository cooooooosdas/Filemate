# FileMate 环境变量配置
# 复制本文件为 .env 并填入真实值
# .env 已被 .gitignore 排除，不会提交到 Git

# ── LLM 配置（必填）──
# 可从 StepFun 官网 https://platform.stepfun.com 注册获取
LLM_PROVIDER=auto
LLM_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LLM_BASE_URL=https://api.stepfun.com/v1
LLM_MODEL=step-3.7-speed

# ── 本地数据路径（可选）──
# 未配置时，数据默认写入项目根目录的 .filemate-data/，归档写入 archive/。
# 建议团队成员只在需要隔离开发数据时取消注释；请使用各自机器上的绝对路径。
# FILEMATE_DATA_DIR=C:/path/to/filemate-data
# FILEMATE_UPLOAD_DIR=C:/path/to/filemate-data/inbox
# FILEMATE_DB_PATH=C:/path/to/filemate-data/filemate.db
# FILEMATE_ARCHIVE_DIR=C:/path/to/filemate-archive

# 模拟面试仅使用本地规则评分，不把回答发送给模型（1=开启）
# FILEMATE_INTERVIEW_LOCAL_ONLY=1

# 桌面壳层关闭本地 Sidecar 时使用；普通本地开发不需要设置。
# FILEMATE_SHUTDOWN_TOKEN=replace-with-a-random-local-token
